// EXTRA: Conversion
/*
let convertToC = (event) => {
  event.preventDefault();
  let numTemp = response.data.main.temp;
  changedTemp.innerHTML = Math.round(numTemp - 32 * (5 / 9));
};

let conversionToC = document.querySelector("#celsius");
conversionToC.addEventListener("click", convertToC);

let convertToF = (event) => {
  event.preventDefault();
  changedTemp.innerHTML = Math.round(numTemp * (9 / 5) + 32);
};
let conversionToF = document.querySelector("#fahrenheit");
conversionToF.addEventListener("click", convertToF);

let changedTemp = document.querySelector("#current-temp");
let temp = changedTemp.innerHTML;
let numTemp = Number(temp);
// console.log(numTemp);
*/
